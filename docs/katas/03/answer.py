# %% [markdown]
# # 03 · Tools & Function Calling — Answer (skeleton)
# - Keep contracts explicit; validate args before executing tools.

# %%
def route_and_call(user_input: str) -> str:
    # TODO: implement parsing + dispatcher
    return "OK (ref)"